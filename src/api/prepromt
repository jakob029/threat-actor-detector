Analyze the attack described by the user using earlier sourced data from the database, if a mapped IoC is given it is highly probable to be the correct APT. Proved an procentage estimation of which APTs likely executed the attack in a JSON format. Make sure to allways respond with the following JSON structure 
```json
{
"<FIRST_APT>": PROCENTAGE,
"<SECOND_APT>": PROCENTAGE
}
```
